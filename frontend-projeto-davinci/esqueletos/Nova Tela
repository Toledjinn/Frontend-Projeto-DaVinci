// import React from 'react';
// import { View, Text, SafeAreaView, ScrollView, useWindowDimensions } from 'react-native';
// import { useFocusEffect } from 'expo-router';
// import { styles } from './NewScreen.styles'; // Importa os estilos
// import { useUIStore } from '@/state/uiStore';
// import Chefinho from '@/assets/characters/chefinho.svg'; // Exemplo de personagem

// export default function NewScreen() {
//   const setHeaderConfig = useUIStore((state) => state.setHeaderConfig);
//   const { height } = useWindowDimensions();
//   const headerHeight = height * 0.27; // Altura do header para o padding

//   useFocusEffect(
//     React.useCallback(() => {
//       // Configure o Header para esta tela
//       setHeaderConfig({
//         layout: 'page', // 'page' para telas internas, 'home' para a principal
//         showPageHeaderElements: true,
//         pageTitle: 'Título da Tela', // Mude o título aqui
//         CharacterSvg: Chefinho, // Mude o personagem SVG aqui
//         showNotificationIcon: true, // Defina se o sino de notificação aparece
//       });
//     }, [])
//   );


//   return (
//     <SafeAreaView style={styles.safeArea}>
//       <ScrollView 
//         style={styles.scrollView}
//         contentContainerStyle={[styles.contentContainer, { paddingTop: headerHeight }]}
//       >
//         <Text style={styles.title}>Conteúdo da Nova Tela</Text>
//         {/* Adicione aqui os outros componentes da sua tela */}
//       </ScrollView>
//     </SafeAreaView>
//   );
// }
